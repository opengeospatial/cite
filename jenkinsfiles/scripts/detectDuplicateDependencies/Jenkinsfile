pipeline {
  agent any
  tools {
    maven 'mvn'
    jdk 'JDK 8'
  }
  stages {
    stage('Test') {
      steps{
        sh './scripts/detectDuplicateDependencies.sh'
      }
    }
    stage('Clean up') {
      steps{
        deleteDir()
      }
    }
  }
}
